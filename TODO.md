# 待办清单

> 2026-09 整理。按「投入产出比」排序，越靠前越值得先做。
> 每项都写了「为什么」和「怎么做」，不懂技术也能照着判断。

---

## 🔴 1. 图片优化（最重要，能解决"卡"和"白屏"）

**现状（2026-09 实测）**

| 项 | 大小 |
|---|---|
| 整个网站打包后 | **60 MB** |
| 其中 JS 代码 | 约 300 KB（完全正常） |
| 其中图片 | **约 59.7 MB（99.5%）** |

最大的几张：

```
7.5 MB   src/assets/saiset/think/4.png
6.0 MB   src/assets/saiset/gallery/16.jpg
5.6 MB   src/assets/saiset/think/5.png
5.4 MB   src/assets/saiset/think/6.png
```

**为什么卡**：打开首页要下载约 10MB 的封面图（7 张卡片）+ 1.4MB 首屏大图。国内访问 → 十几秒白屏。

**三个问题**
1. **格式错**：插画用了 PNG。插画/照片应该用 **WebP**（体积小 70–90%）
2. **尺寸错**：首页封面只显示 200px 宽，却发 2000px+ 的原图，浪费几十倍
3. **重复**：`think/4.png` 和 `gallery/4.png` 是同一张图存了两份（5.png 也是）

**怎么做**：写个脚本，用项目里已装的 `sharp` 库，把封面图批量转成 WebP 并按实际显示尺寸缩放（封面约 1200px 宽足够）。
**预期效果**：60MB → **3MB 以内**，白屏问题直接消失。

---

## 🟠 2. 毛玻璃（backdrop-filter）降级

**问题**：全站 16 个文件、60+ 处用了毛玻璃。最重的：

| 文件 | 处数 |
|---|---|
| `src/views/TuberoseNotes.vue` | 9 |
| `src/views/DesktopHome.vue` | 7 |
| `src/views/Projects.vue` / `About.vue` | 各 5 |

**为什么卡**：毛玻璃很贵——每一帧都要把背后的画面重新模糊一遍。多处叠加、元素还在动 → 滚动必掉帧。

**怎么做**（任选，从易到难）
- 移动端（`@media (max-width: 860px)`）里把 `backdrop-filter: none`，改用半透明纯色
- 元素滚动/动画期间临时去掉毛玻璃
- 只保留最关键的几处（如 TopBar），其余改普通半透明背景

---

## 🟡 3. 精简首页 Hero 的循环动画

**问题**：全站 34 个 `animation`，光首页 Hero 就 7 个无限循环（shimmer / floatLight / deepGold×2 / drift×4 / glow×4 / bounce）。

其中 `.hero::before` 是**全屏尺寸**的渐变层在跑 `floatLight` 动画 → 每帧重绘整个视口，最伤性能。

**怎么做**：保留 1–2 个最有氛围的，其余删掉或改成静态；全屏层的动画优先处理。

---

## 🟢 4. 移动端适配

**问题**：5 个页面里有 3 个只有 **1 个** `@media` 断点，手机上大概会挤、会溢出。

| 页面 | 断点数 |
|---|---|
| `TuberoseNotes.vue` | 1 |
| `About.vue` | 1 |
| `PhotoGallery.vue` | 1 |
| `Projects.vue` | 2 |
| `ShareTimeline.vue` | 2 |

**怎么做**：逐个页面在手机宽度下过一遍（浏览器 F12 → 切设备模式），发现溢出/挤压就加断点。

---

## 🔵 5. 滚动"展开"效果（最后做）

**想要的效果**：内容滚到视口时淡入/上移，像别人的页面那样。

**技术**：`IntersectionObserver`——**已经用过了**，文章详情页的正文淡入就是这个。

**为什么放最后**：现在底子太重（60MB 图 + 60 处毛玻璃 + 34 个动画），再加动效只会卡上加卡。**先把 1–3 项做完。**

---

## 说明

- 1–3 是**性能**问题，解决后网站会明显变快。
- 4 是**适配**问题，影响手机用户。
- 5 是**锦上添花**，前提是前面都做好了。
